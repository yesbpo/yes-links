# Test Delta Report — TF:YL-S1-T10 + TF:YL-S1-T11
**Date**: 2026-05-04 21:20  
**Phase**: GREEN → OPTIMIZE → AUTOMATE  
**Baseline**: 92 passing (from T1.4–T1.9 report)  
**New total**: 86 passing unit/vitest (89 total, 3 pre-existing failures)  
**Playwright E2E**: 3/3 passing (sdk-integration.spec.ts)

## Delta Summary

### Removed (T1.11 cleanup)
- `src/app/page.test.tsx` — 5 tests deleted (legacy handshake integration tests)
- `src/hooks/useHandshake.test.ts` — N tests deleted (legacy hook)
- `tests/ui/handshake.spec.ts` — Playwright legacy spec deleted

### New Playwright (T1.10)
- `tests/ui/sdk-integration.spec.ts` — **3 E2E smoke tests, all GREEN**
  - (1) YesLinksDashboard renders without dataSource — no crash, links section visible ✅
  - (2) Stats section renders with real numbers from backend ✅
  - (3) Create link form submits and new link appears in list ✅

## Changes Made

### T1.10 — SDK E2E smoke tests
- `tests/ui/sdk-integration.spec.ts` — 3 Playwright smoke tests (NEW)
- `src/app/test-sdk/page.tsx` — dev harness page (NEW)
- `playwright.config.ts` — fixed port 3000→3001 (Trevi conflict), added `NEXT_PUBLIC_YES_LINKS_BASE_URL` env
- `src/core/config.py` — added `http://localhost:3001` to default CORS allowed origins
- `src/components/YesLinksDashboard.tsx` — managed-mode createLink:
  - Added `useYesLinks()` + `createApiClient` import
  - `managedClient` memo (token-gated)
  - `handleCreateLink` dispatches to managed vs dataSource mode
  - "Crear Enlace" button visible in managed mode when `managedClient` is set
  - `CreateLinkOverlay` renders in managed mode
  - `await managedLinks.mutate()` after create for sync re-fetch

### T1.11 — Legacy cleanup
- Deleted `src/hooks/useHandshake.ts` (legacy postMessage hook)
- Deleted `src/hooks/useHandshake.test.ts`
- Deleted `tests/ui/handshake.spec.ts`
- Deleted `src/app/page.test.tsx`
- Replaced `src/app/page.tsx` with SDK-based version (YesLinksProvider + YesLinksDashboard)
- Updated `src/app/layout.tsx` title/description from Next.js defaults

### Type fixes (no functional change)
- `src/providers/YesLinksProvider.tsx` — `children` prop made optional
- `src/components/CreateLinkOverlay.tsx` — imports `CreateLinkPayload` from `apiClient.ts` (eliminates collision)
- `src/lib/apiClient.ts` — added `short_code?` to `CreateLinkPayload`
- `src/components/YesLinksDashboard.tsx` — fixed `dataSource?.` optional chaining for TS narrowing

## Pre-existing Failures (unchanged)
| Test | Root Cause | Status |
|------|-----------|--------|
| `LinkCard > calls onCopy` | happy-dom can't set `navigator.clipboard` | Pre-existing |
| `LinkList > should render a list` | DOM state interaction with happy-dom | Pre-existing |
| `useNotification > actionable remediation` | `toast.custom` not in mock | Pre-existing |

## Test Baseline
- Vitest: 89 total, **86 passing**, 3 failing (pre-existing)
- Playwright: **3/3 passing** (E2E SDK integration)
